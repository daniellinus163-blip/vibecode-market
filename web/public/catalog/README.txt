Catalog cloth images for /api/local-image?id={id}

Bundled PNGs use IDs from catalogImages.ts: 1, 2, 8, 9, and 11–26.

To replace one image, overwrite web/public/catalog/{id}.png (or rely on .jpg/.jpeg/.webp via the API route).
